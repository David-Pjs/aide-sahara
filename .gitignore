# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# secrets & runtime state
.env
.env.local
account.json

# logs & os
*.log
.DS_Store
Thumbs.db

# typescript build info
*.tsbuildinfo

# IDE / agent caches
.claude/

# Python virtual environments
tts-env/
venv/
.venv/
.vercel

# Throwaway build dirs used by side-by-side test servers (see AIDE_DIST_DIR)
.next-*/

# Python bytecode from the TTS worker / speak function
__pycache__/
*.pyc

# Raw HuggingFace shards and extraction intermediates for the benchmark
# (~419MB, one file alone is 158MB — well past GitHub's 100MB limit). Only
# benchmark/samples/ (the 4 trimmed clips actually referenced by
# manifest.json) is meant to ship with the submission.
benchmark/raw/

# Pitch deck, demo scripts and personal photos: not part of the submission repo
_pitch/
